using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Memo : MonoBehaviour
{

    // Debug.DrawLine(transform.position, transform.position + new Vector3(0, -coll.size.y / 2), Color.blue);
    // Debug.DrawLine(); 디버그도 체크용도로 씬카메라에 선을 그려줄수 있음
    // Gizmos.DrawSphere(); 디버그 보다 더 많은 시각효과를 제공
    // UnityEditor.Handles.DrawLine();



    /*
     * Layer int로 대상의 레이어를 구분
     * Layer의 int와 공통적으로 활용하는 int와 다름
     * Wall Layer, Ground Layer
     */




}
